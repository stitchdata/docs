---
tap: "zuora"
version: "1"

name: "billingRun"
doc-link: https://live-www.zuora.com/developer/api-reference/#tag/billing-Run
#singer-schema: 
description: |
  The `{{ table.name }}` table contains information about billing runs.

replication-method: "Key-based Incremental"
api-method:
  name:
  doc-link:

attributes:
  - name: "id"
    type: "string"
    primary-key: true
    description: "The ID of the billing run."

  - name: "updatedDate"
    type: "date-time"
    replication-key: true
    description: "The date when the billing run was last updated."

  - name: "autoEmail"
    type: "boolean"
    description: |
      **Note**: Only available if the Support Billing Run Auto-Post Billing Rule is enabled.

      If `true`, email invoices will auto-send when the billing run completes.

  - name: "autoPost"
    type: "boolean"
    description: |
      **Note**: Only available if the Support Billing Run Auto-Post Billing Rule is enabled.

      If `true`, the billing run will auto-post upon completion.

  - name: "autoRenewal"
    type: "boolean"
    description: "Indicates if auto-renew subscriptions should be determined by the billing run."

  - name: "batch"
    type: "string"
    description: "The batch of accounts for the billing run."

  - name: "billCycleDay"
    type: "string"
    description: "The day of the billing cycle."

  - name: "billRunNumber"
    type: "string"
    description: "The billing run number."

  - name: "chargeTypeToExclude"
    type: "string"
    description: "The charge type(s) to be excluded from the billing run."

  - name: "createdById"
    type: "string"
    description: "The ID of the {{ integration.display_name }} user who created the billing run."

  - name: "createdDate"
    type: "date-time"
    description: "The date when the billing run was created."

  - name: "deleted"
    type: "boolean"
    description: |
      **Only supported for the AQuA API.** If `true`, this record was deleted in {{ integration.display_name }}.

  - name: "errorMessage"
    type: "string"
    description: "The error message for the billing run."

  - name: "executedDate"
    type: "date-time"
    description: "The execute date for the billing run."

  - name: "invoiceDate"
    type: "date-time"
    description: "The invoice date for the billing run."

  - name: "invoicesEmailed"
    type: "boolean"
    description: "If `true`, the invoice has been sent."

  - name: "lastEmailSentTime"
    type: "date-time"
    description: "The last time emails were sent for the billing run."

  - name: "noEmailForZeroAmountInvoice"
    type: "boolean"
    description: "If `true`, invoices with a zero total (`0`) will not recieve an invoice for the billing run."

  - name: "numberOfAccounts"
    type: "integer"
    description: "The number of accounts processed by the billing run."

  - name: "numberOfInvoices"
    type: "integer"
    description: "The number of invoices generated by the billing run."

  - name: "status"
    type: "string"
    doc-link: "https://knowledgecenter.zuora.com/CB_Billing/J_Billing_Operations/G_Bill_Runs#Status_Types"
    description: |
      The status for the billing run. Possible values are:

      - `Pending`
      - `Processing`
      - `Completed`
      - `Error`
      - `Canceled`
      - `Posted`

  - name: "targetDate"
    type: "date-time"
    description: "The target date of the billing run."

  - name: "updatedById"
    type: "string"
    description: "The ID of the {{ integration.display_name }} user who last updated the billing run."
---