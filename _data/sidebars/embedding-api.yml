all-docs:
  - title: sidebar
    categories:
      - title: "{{ page.api-name }}"

      - title: Introduction
        url: "{{ page.anchors.introduction }}"
        drop-down: false

      - title: Quick Start
        url: "{{ page.anchors.quick-start }}"
        drop-down: false

      - title: "Authentication"
        url: "{{ page.anchors.authentication }}"
        drop-down: false

      - title: "Response Codes"
        url: "{{ page.anchors.response-codes }}"
        drop-down: false

      - title: "Versioning"
        url: "{{ page.anchors.versioning }}"
        drop-down: false

      - title: "Core Objects"
        expand-by-default: true
        sections:
          - title: "Accounts"
            expand-by-default: true
            subsections:
              - title: "Create an account"
                url: "{{ page.anchors.core-objects.accounts.create-an-account }}"

          - title: "Destinations"
            expand-by-default: true
            subsections:
              - title: "The destination object"
                url: "{{ page.anchors.core-objects.destinations.object }}"

              - title: "Create a destination"
                url: "{{ page.anchors.core-objects.destinations.create-a-destination }}"

              - title: "Update a destination"
                url: "{{ page.anchors.core-objects.destinations.update-a-destination }}"

              - title: "List destinations"
                url: "{{ page.anchors.core-objects.destinations.list-destinations }}"

          - title: "Sessions"
            expand-by-default: true
            subsections:
              - title: "The session object"
                url: "{{ page.anchors.core-objects.sessions.object }}"

              - title: "Create a session"
                url: "{{ page.anchors.core-objects.sessions.create-a-session }}"

          - title: "Sources"
            expand-by-default: true
            subsections:
              - title: "The source object"
                url: "{{ page.anchors.core-objects.sources.object }}"

              - title: "Create a source"
                url: "{{ page.anchors.core-objects.sources.create-a-source }}"

              - title: "Update a source"
                url: "{{ page.anchors.core-objects.sources.update-a-source }}"

              - title: "Retrieve a source"
                url: "{{ page.anchors.core-objects.sources.retrieve-a-source }}"

              - title: "List sources"
                url: "{{ page.anchors.core-objects.sources.list-sources }}"

          - title: "Source Types"
            expand-by-default: true
            subsections:
              - title: "The source type object"
                url: "{{ page.anchors.core-objects.source-types.object }}"

              - title: "Get a source type"
                url: "{{ page.anchors.core-objects.source-types.get-a-source-type }}"

              - title: "List source types"
                url: "{{ page.anchors.core-objects.source-types.list-source-types }}"

      - title: "Data Structures"
        sections: 
          - title: "Connection Checks"
            url: "{{ page.anchors.data-structures.connection-checks }}"

          - title: "Connection Steps"
            url: "{{ page.anchors.data-structures.connection-steps }}"

          - title: "Current Step Hints"
            url: "{{ page.anchors.data-structures.current-step-hints }}"

          - title: "Properties"
            url: "{{ page.anchors.data-structures.properties }}"

          - title: "Report Cards"
            url: "{{ page.anchors.data-structures.report-cards }}"

      - title: "Form Properties"
        sections:
          - title: "Destination Forms"
            subsections:
              - title: "PostgreSQL"
                url: "{{ page.anchors.form-properties.destination-forms.postgresql }}"

              - title: "Redshift"
                url: "{{ page.anchors.form-properties.destination-forms.redshift }}"

              - title: "Snowflake"
                url: "{{ page.anchors.form-properties.destination-forms.snowflake }}"

          - title: "Source Forms"
            subsections:
              - title: "HubSpot"
                url: "{{ page.anchors.form-properties.source-forms.hubspot }}"

              - title: "Marketo"
                url: "{{ page.anchors.form-properties.source-forms.marketo }}"

              - title: "Salesforce"
                url: "{{ page.anchors.form-properties.source-forms.salesforce }}"

      - title: "Stitch.js"
        sections:
          - title: "addSource"
            url: "{{ page.anchors.stitch-js.create-a-source }}"

          - title: "authorizeSource"
            url: "{{ page.anchors.stitch-js.authorize-a-source }}"

          - title: "displayDiscovery"
            url: "{{ page.anchors.stitch-js.source-discovery }}"

          - title: "selectStreamsForSource"
            url: "{{ page.anchors.stitch-js.select-streams }}"

          - title: "editSource"
            url: "{{ page.anchors.stitch-js.edit-source }}"
